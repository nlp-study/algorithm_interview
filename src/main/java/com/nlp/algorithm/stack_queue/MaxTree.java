package com.nlp.algorithm.stack_queue;

public class MaxTree {
	public void buildMaxTree()
	{
		
	}
	
	public void  processMaxTree(int[]A,int i)
	{
		int leftIndex = i * 2;
		int rightIndex = i * 2 +1;
		
		if(leftIndex < A.length && A[leftIndex]<A[i])
		{
			
		}
		else if(rightIndex < A.length && A[leftIndex]<A[i])
		{
			
		}
	}

}
